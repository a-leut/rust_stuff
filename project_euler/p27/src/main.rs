/*fn gen_primes(limit: u32) {
	let isp [bool; limit] = [true; limit];
	for n in isp {

	}
	isp.map(|)
	return p;
}

fn max_prime_seq(a: i32, b: i32) -> i32 {
	32
}*/

fn main() {
	//let primes = gen_primes(10000);
    for a, b in (-999..1000).zip((-1000..1001)) {
		println!("{} {}", a, b);
	}
}
